<resources xmlns:tools="http://schemas.android.com/tools">
  <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
    <item name="android:enforceNavigationBarContrast" tools:targetApi="29">true</item>
    <item name="android:editTextBackground">@drawable/rn_edit_text_material</item>
    <item name="colorPrimary">@color/colorPrimary</item>
    <item name="android:statusBarColor">#ffffff</item>
  </style>
  <style name="Theme.App.SplashScreen" parent="Theme.AppCompat.DayNight.NoActionBar">
    <!-- Use window background drawable for splash screen -->
    <item name="android:windowBackground">@drawable/splash_screen</item>
    <!-- Match status bar color to splash background -->
    <item name="android:statusBarColor">@color/splashscreen_background</item>
    <!-- Disable window content transitions for instant display -->
    <item name="android:windowDisablePreview">false</item>
  </style>
</resources>